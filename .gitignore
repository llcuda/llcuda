# Python
__pycache__/
*.py[cod]
*$py.class
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# ============================================================================
# CRITICAL: llcuda Distribution Strategy
# ============================================================================
# Git Repo:    Keep code-only, no binaries (~1-2 MB)
# PyPI Wheel:  Code only, users download binaries on first import (~50-70 KB)
# GitHub Releases: Binary packages for manual download (~266 MB each)
# ============================================================================

# Exclude dist/ but we'll build PyPI wheels without binaries
dist/
build/
*.whl
*.tar.gz
*.tar.xz
*.tar.bz2
*.zip
*.7z

# Large binaries - NEVER in git repo
llcuda/binaries/
llcuda/lib/
llcuda/models/
llcuda/_internal/binaries/
llcuda/_internal/lib/
*.so
*.so.*
*.pyd
*.dylib
*.dll
*.a
llcuda_cpp*.so
libggml*.so
libllama*.so
libggml-*.so

# Model files - NEVER commit (licensed models, too large)
*.gguf
*.bin
*.safetensors
*.pt
*.pth
*.onnx
models/
*.model

# Build/compilation artifacts
compile/
temp_extract/
builds/
.cmake

# Release/packaging (prepare these separately)
release-packages/
releases/
bundles/
check_binaries/

# Backup and temporary files
*_backup.py
*_old.py
*.bak
*.orig
tmp/
temp/
test_outputs/

# Large archives/packages - only for GitHub Releases
llcuda-binaries-*.tar.gz
llcuda-bins-*.tar.gz
llcuda-v*.tar.gz
llcuda-complete-*.tar.gz
